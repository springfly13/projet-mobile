<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ioweb.concertplace">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="MA PLACE DE CONCERT"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ArtistesFavoriteActivity"></activity>

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".ModifyFavoriNameActivity"
            android:label="MA PLACE DE CONCERT" />

        <activity android:theme="@android:style/Theme.DeviceDefault.Light.Dialog"
            android:name=".AddFavoriActivity"
            android:label="MA PLACE DE CONCERT" />
        <activity android:name=".NewActivity" />
        <activity android:name=".ArtistesSortActivity" />

    </application>

</manifest>